services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - media_data:/app/media
      - db_data:/app/data
    environment:
      DJANGO_DB_NAME: /app/data/db.sqlite3

volumes:
  media_data:
  db_data:
